Headings: <h1> to <h6> are used for headings, with <h1> being the most important and <h6> the least.
<h1>Main Heading</h1>
<h2>Subheading</h2>
<h3>Subheading up to h6>

Paragraph: <p> is used to define paragraphs.
<p>This is a paragraph.</p>
  
Links: <a> is used to create hyperlinks.
<a href="https://www.example.com">Visit Example.com</a>
  
Images: <img> is used to embed images.
<img src="image.jpg" alt="A description of the image">
  
Lists: <ul> for unordered lists, <ol> for ordered lists, and <li> for list items.

<ul>
  <li>Item 1</li>
  <li>Item 2</li>
</ul>
  
Tables: <table> for tables, with <tr> for table rows, <td> for table data, and <th> for table headers.

<table>
  <tr>
    <th collspan="2">Header 1</th>
    <th>Header 2</th>
  </tr>
  <tr>
    <td rowspan="2">Data 1</td>
    <td>Data 2</td>
  </tr>
</table>.


Forms:

HTML forms are used to collect user input. The <form> element wraps input elements like <input>, <textarea>, <button>, and <select>.
Example:
<form action="/submit" method="POST">
  <label for="name">Name:</label>
  <input type="text" id="name" name="name">
  <button type="submit">Submit</button>
</form>


  Semantic HTML:

Semantic HTML uses elements that clearly describe their meaning in a human- and machine-readable way. Examples include <header>, <footer>, <article>, and <section>.
Example:
<header>
<a href=" to naginate the page and to aquire the praparties  from out side styling>
  <h1>Website Header</h1>
</header>
<article>
  <h2>Article Title</h2>
  <p>This is an article. it reprasents indivisual content peace ccontent like blog </p>
</article>
<footer>
  <p>Website Footer</p>
</footer>
